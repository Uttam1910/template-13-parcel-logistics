# Parcel — Logistics, without the guesswork.
#
# Public base URL used for canonical links, Open Graph metadata, sitemap and robots.
# No secrets are required by this template: it is a fully local demo environment with
# no database, carrier API, tracking API, map provider, authentication provider or
# payment provider.
NEXT_PUBLIC_APP_URL=http://localhost:3000
